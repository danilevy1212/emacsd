;; danilevy1212 emacs libs.

;; TODO set up emacs-libs repo
(defvar libs/load-libraries 'all
  "Libraries that will be loaded from danilevy/emacs-libs."
  :choices '(all))


(defun my/load-libs ()
  "Loads the elisp files "
  )

(provide 'libs)
;;; libs.el ends here
